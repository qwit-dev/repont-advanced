// leaflet
import { MapContainer, TileLayer, useMap } from 'react-leaflet'

const MainIndexPage = () => {
    return (
        <div>
            <p>refasz</p>
        </div>
    );
}

export default MainIndexPage;